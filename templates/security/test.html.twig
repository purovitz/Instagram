
{% for post in posts %}

    <br>

    <div class="container">
        <div class="row card-body">
            <div class="col-md-12 ">
                <div class="wrapper text-center">
                    <img src="/images/hosting/{{ post.image }}" class="card-img-top mt-4 image" alt="..." style="width:17cm; height: 5cm">
                    <div class="container">
                        <div class="card mt-5">
                            <div class="card-body ">
                                <p>{{post.content}}</p>
                            </div>
                            <div class="w3-col m8 s12">
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endfor %}







<nav class="navbar">
    <div class="nav-wrapper">
        <img src="{{ asset('assets/img/logo.PNG') }}" class="brand-img" alt="">
        <input type="text" class="search-box" placeholder="search">
        <div class="nav-items">
            <a href="{{ path('app_dashboard') }}"><img src="{{ asset('assets/img/home.PNG') }}" class="icon" alt=""></a>
            <img src="{{ asset('assets/img/messenger.PNG') }}" class="icon" alt="">
            <a href="{{ path('create_post') }}"><img src="{{ asset('assets/img/add.PNG') }}" class="icon" alt=""></a>
            <img src="{{ asset('assets/img/explore.PNG') }}" class="icon" alt="">
            <img src="{{ asset('assets/img/like.PNG') }}" class="icon" alt="">
            <div class="icon user-profile"></div>
        </div>
    </div>
</nav>





<body>
<!-- partial:index.partial.html -->
<br><br>
<br><br>
<div class="box-form ">
    <div class="left">
        <div class="overlay">
            <h1>My Stagram</h1>
            <h1></h1>
            <p class="text-center">(((My second symfony project)))</p>
            <span>

    </span>
        </div>
    </div>


    <div class="right ">
        <h1>Reset your password</h1>

        {{ form_start(resetForm) }}
        {{ form_row(resetForm.plainPassword) }}
        <button class="btn btn-primary">Reset password</button>
        {{ form_end(resetForm) }}
    </div>

</div>
<!-- partial -->

</body>






<h1>Reset your password</h1>

{{ form_start(requestForm) }}
        {{ form_row(requestForm.email) }}
<div>
    <small>
        Enter your email address and we will send you a
        link to reset your password.
    </small>
</div>

<button class="btn btn-primary">Send password reset email</button>
{{ form_end(requestForm) }}




<h1 class="h3 mb-3 font-weight-normal">Please sign in</h1>
<label for="inputEmail">Email</label>
<input type="email" value="{{ last_username }}" name="email" id="inputEmail" class="form-control" autocomplete="email" required autofocus>
<label for="inputPassword">Password</label>
<input type="password" name="password" id="inputPassword" class="form-control" autocomplete="current-password" required>

<input type="hidden" name="_csrf_token"
       value="{{ csrf_token('authenticate') }}"
>

{#
Uncomment this section and add a remember_me option below your firewall to activate remember me functionality.
See https://symfony.com/doc/current/security/remember_me.html

<div class="checkbox mb-3">
    <label>
        <input type="checkbox" name="_remember_me"> Remember me
    </label>
</div>
#}

<button class="btn btn-lg btn-primary" type="submit">
    Sign in
</button>
</form>




checkemail.html.teig

<p>
    If an account matching your email exists, then an email was just sent that contains a link that you can use to reset your password.
    This link will expire in {{ resetToken.expirationMessageKey|trans(resetToken.expirationMessageData, 'ResetPasswordBundle') }}.
</p>
<p>If you don't receive an email please check your spam folder or <a href="{{ path('app_forgot_password_request') }}">try again</a>.</p>


ostatnie juz

<h1>Reset your password</h1>

{{ form_start(resetForm) }}
        {{ form_row(resetForm.plainPassword) }}
<button class="btn btn-primary">Reset password</button>
{{ form_end(resetForm) }}







<!--NAV-->
<nav class="navbar navbar-expand-lg navbar-dark dupa">
    <div class="container">
        <a class="navbar-brand" href="{{ path('app_dashboard') }}">MyStagram</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor02" aria-controls="navbarColor02" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse mt-3 m-lg-1" id="navbarColor02">
            <ul class="navbar-nav mr-auto">



                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                        Options
                    </a>
                    <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                        <a class="dropdown-item" href="{{ path('app_logout') }}">Wyloguj się</a>

                    </div>

                </li>

            </ul>
        </div>
    </div>
</nav>

